# AppBlocker Client — самостоятельная программа, зависимости отдельные от Source/.

# Интерфейс (минимальный — окно почти всегда скрыто в трее) и трей.
flet>=0.86,<0.87
pystray>=0.19
pillow>=10.0

# Мониторинг и завершение процессов.
psutil>=7.0

# WAN-режим: соединение с relay-сервером.
websockets>=12.0

# Сборка бинарника (только для разработки).
pyinstaller>=6.0
