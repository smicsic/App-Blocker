# Интерфейс. flet-desktop приходит зависимостью и содержит только загрузчик
# клиента: сам flet.exe (~100 МБ) скачивается при первом запуске или кладётся
# папкой рядом с exe — см. раздел про клиент Flet в README.
flet>=0.86,<0.87

# Мониторинг и завершение процессов.
psutil>=7.0

# Иконка в трее и подготовка изображения для неё.
pystray>=0.19
pillow>=10.0

# Remote Admin: WAN-режим через relay-сервер.
websockets>=12.0

# Сборка exe (только для разработки).
pyinstaller>=6.0
