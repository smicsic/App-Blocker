# Интерфейс. flet-desktop приходит зависимостью и содержит только загрузчик
# клиента: сам клиент flet (~100 МБ) скачивается при первом запуске или кладётся
# папкой рядом с программой — см. раздел про клиент Flet в README.
flet>=0.86,<0.87

# Мониторинг и завершение процессов.
psutil>=7.0

# Иконка в трее и подготовка изображения для неё. На Linux pystray использует
# GTK/AppIndicator: нужны системные пакеты, например на Debian/Ubuntu —
# `sudo apt install python3-gi gir1.2-appindicator3-0.1` (или
# gir1.2-ayatanaappindicator3-0.1 на более новых версиях).
pystray>=0.19
pillow>=10.0

# Сборка бинарника (только для разработки).
pyinstaller>=6.0
